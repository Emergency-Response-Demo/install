---
kind: ImageStream
apiVersion: image.openshift.io/v1
metadata:
  name: {{ imagestream_name }}
  labels:
    app: {{ application_name }}
spec:
  dockerImageRepository: {{ kafka_consumer_app_image }}